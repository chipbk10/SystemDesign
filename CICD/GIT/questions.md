1. @Todo
2. What happens when we do `git pull origin develop` on the feature branch?
3. Can we do `git pull origin develop` using ff-only strategy on the feature branch?
4. What merge strategy should be used when merging a feature branch to develop branch?
